# Render Blueprint for GaanaNextleap backend only
# Frontends are deployed separately to Vercel.

services:
  - type: web
    name: gaana-phase3-backend
    env: node
    plan: free
    branch: main
    buildCommand: cd phase3/backend && npm ci && npm run build
    startCommand: cd phase3/backend && npm run start
